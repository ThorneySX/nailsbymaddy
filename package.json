{
  "name": "nailsbymaddy",
  "private": true,
  "description": "nailsbymaddy.co.uk — one-page site on a Cloudflare Worker",
  "scripts": {
    "build": "python3 build_site.py",
    "verify": "python3 verify.py",
    "check": "npm run build && npm run verify",
    "ci": "python3 build_site.py && python3 verify.py --no-browser",
    "deploy": "npm run check && wrangler deploy",
    "preview": "npm run build && python3 -m http.server 8787 --directory public"
  },
  "dependencies": {
    "@fontsource/outfit": "^5.3.0",
    "@fontsource/shrikhand": "^5.3.0"
  },
  "devDependencies": {
    "svgo": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
