The controls for this repo live in **[AGENTS.md](AGENTS.md)**. Read that first.

It is a real file rather than a symlink because this repo is cloned on Windows,
where symlinks need a setting most checkouts do not have — a broken link here
would mean an agent silently reads nothing.
